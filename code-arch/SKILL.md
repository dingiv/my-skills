---
name: code-arch
description: 从零到 N 实现完整应用的**可执行架构操作规程**，以「daemon 程序的标准形态」为理论基础（main + 启动三件套 + 事件循环 + handler 分发 + 清理 / 三层单向依赖结构），主张「程序即状态机、需求即端到端链路、复杂度由状态机封装」。方法论分四步——需求阐述（定义端到端入口）→ 架构设计（端到端入口设计 / 接口定义伪代码 / 依赖寻找驱动时序 / 空间复杂度优化）→ 时序约束（依赖前向包含 / 空间封装 / 逻辑时序 / 工程管理）→ AI 应用视角（上下文受限、拆分能力是应用 AI 的关键）。**可应用于三种典型场景：架构新项目（0→1）、架构重构（已有项目改造）、架构审视（in-progress 项目的架构 review）**——三种场景的入口、方向、产出都不同，误用会得到错误的产物。在以下场景推荐使用——用户提出"做一个新项目 / 从零设计 X / 帮我搭一个 Y 系统"时启动 Bootstrap 流程；用户提出"重构现有项目 / 这块设计有问题"时启动架构重构流程；用户提出"看看这个项目的架构怎么样"时启动架构审视流程；为复杂需求设计模块拆分、定义端到端调用链路、解释「为什么这么分层」、与 calltree / rust-style / ts-style skill 协同使用（code_arch 决定架构层，calltree 描述链路层，语言 style 决定代码层）。推荐做法——先定需求再定入口，先定入口再定接口，先定接口再定依赖，先定依赖再优化空间；任何代码改动前先问「它属于哪条端到端链路」「它影响了什么状态」「它的生命周期是什么」；不要跳过任何流程步骤，每步必须向用户确认才能进入下一步。关键词：项目架构、架构设计、daemon 形态、main + 事件循环 + handler、端到端、状态机、复杂度原理、空间复杂度、时序复杂度、生命周期、依赖方向、单向依赖、单程序 MVC、事件循环、接口优先、伪代码驱动、AI 上下文、需求驱动、Bootstrap、可执行、操作规程、新项目引导。
---

# 从零到 N 实现完整应用

> **程序即状态机；需求即端到端链路；架构 = 把指数级的状态空间封装成线性可管理的范围；先有空间再有时间。**

本 skill 是**可执行的项目架构操作规程**——面对「做一个新项目」「重构现有项目」「review 项目架构」这三类请求，按明确步骤引导，**不会上来就开始写代码**。架构一个程序的前提是**先知道程序应该长什么样**——这一标准形态在 [anatomy](references/anatomy.md) 中给出（daemon 八件套：main + 启动三件套 + 事件循环 + handler + 清理；横向三层：View / Service / Model）。

## 一句话

> **架构设计 = 把指数级的状态空间，通过空间封装（模块 / 接口 / 类型 / 生命周期）压回到线性可管理的范围。**

## 如何启动

面对不同请求，按以下入口启动对应流程：

| 请求类型 | 启动流程 |
| --- | --- |
| 「做一个新项目 / 从零设计 X / 帮我搭一个 Y 系统」 | **[Bootstrap 流程](references/bootstrap.md)**：4 步把模糊需求逼成接口契约 |
| 「重构现有项目 / 这块设计有问题 / 改一处崩三处」 | **[模式二·架构重构](references/modes.md#模式二架构重构已有项目--目标架构)**：现状→目标→迁移 |
| 「看看这个项目的架构怎么样 / 给我做架构 review」 | **[模式三·架构审视](references/modes.md#模式三架构审视in-progress-项目)**：现状→评估→行动项 |

> **核心约束**——不要跳过任何流程步骤。每步完成后必须向用户确认才能进入下一步。如果用户主动跳步，礼貌地把当前步骤补完再继续。

## 何时使用

- 面对"做一个新项目"类请求 → 启动 Bootstrap 流程（4 步走完）
- 面对"重构 / 改造现有项目"类请求 → 启动架构重构（先画现状再定目标）
- 面对"review 架构"类请求 → 启动架构审视（只评估不动手）
- 为复杂需求设计模块拆分
- 定义端到端调用链路
- 与 `calltree` / `rust-style` / `ts-style` skill 协同使用

## 内容索引

按需打开：

| 章节 | 内容 | 何时读 |
| --- | --- | --- |
| **[Bootstrap 流程](references/bootstrap.md)** | **对话→需求文档→链路→接口的 4 步流程** | **新项目入口——这是最常用的路径** |
| [核心论断](references/core.md) | 程序即状态机；复杂度封装效应；空间×时序复杂度 | 任何架构动作之前——这是地基 |
| [程序的形态](references/anatomy.md) | **daemon 八件套：main + 启动三件套 + 事件循环 + handler + 清理；横向三层 View/Service/Model** | **架构任何程序之前必读——这是程序的"长相"** |
| [作用域（Scope）](references/scope.md) | **空间如何切分：父子作用域 / 层级 / 全局 vs 局部 / 生命周期 / 所有权** | **拆解复杂需求时——状态空间切分的依据** |
| [域的组合](references/composition.md) | **域里有域（递归/自相似）+ 子状态机组合 + 扩散模型：用约束「雕刻」程序形态** | **理解「为什么能架构」时——约束收敛搜索空间** |
| [容器（Container）](references/container.md) | **切分后如何治理：资源隔离 / 依赖管理 / 通信 / 生命周期；治理边界 + 空间换时间** | **设计模块结构时——容器 = 治理边界** |
| [程序推导流程](references/derivation.md) | **五步推导：需求→链路→接口→事件→handler；接口决定事件源和事件类型** | **从接口契约推导 daemon 实现——连接 bootstrap 和 anatomy 的桥梁** |
| [工作流：四步](references/workflow.md) | 需求→入口→接口→依赖→空间 | 动手做架构设计时——这是操作步骤 |
| [时序约束：四条](references/timing.md) | 依赖前向包含 / 空间封装 / 逻辑时序 / 工程管理 | 检查时序合理性时 |
| [复杂度封装效应](references/encapsulation.md) | 复杂度→封装边界对照表 | 选择封装手段时 |
| [AI 应用视角](references/ai-view.md) | 上下文窗口、拆分能力、AI 工作内存 | 用 AI 辅助架构设计时 |
| [三种应用模式](references/modes.md) | 模式一：新项目 / 模式二：重构 / 模式三：审视 | **判断「今天做哪种架构动作」时先读** |
| [与其他 skill 的关系](references/relations.md) | code_arch / calltree / rust-style / ts-style 的层级 | 与其他 skill 协同时 |
| [自查清单](references/self-check.md) | 按四步拆分的 checkbox | 完成每个里程碑时 |
| [典型反模式](references/anti-patterns.md) | 8 条「反模式 ↔ 纠正」对照 | 怀疑方案走偏时 |

## 选择应用模式（最常问的问题）

不确定该用哪个模式？读 [三种应用模式](references/modes.md) 第一节。判断的快捷问句：

> **「今天我要做的是创造、改造还是评价？」**
> - 创造 → Bootstrap 流程（4 步对话到接口）或 [模式一](references/modes.md#模式一架构新项目0--1)
> - 改造 → [模式二·架构重构](references/modes.md#模式二架构重构已有项目--目标架构)
> - 评价 → [模式三·架构审视](references/modes.md#模式三架构审视in-progress-项目)

> **Bootstrap 流程 vs 模式一**——Bootstrap 流程面向"用户带着模糊需求来"的场景，强制 4 步对话引导；模式一面向"已经有清晰需求文档、动手做架构"的场景。两者衔接：完成 Bootstrap 后进入模式一继续走完第 2.3 / 2.4 步（依赖时序 + 空间优化）。

> **模式选择比方法论本身更重要**——选错了，再好的四步流程也产出错误结果。详细对照见 [三种应用模式](references/modes.md) 末尾的对照表。

## 一句话回顾

> **程序即状态机；需求即端到端链路；架构 = 把指数级的状态空间封装成线性可管理的范围；先有空间再有时间；架构写下来，AI 才能在每一层都落准。**